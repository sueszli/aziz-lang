(defun add_ints (a b)
  (+ a b))
(print (add_ints 10 20))

(defun add_floats (x y)
  (+ x y))
(print (add_floats 3.14 2.86))

(defun multiply_floats (x y)
  (* x y))
(print (multiply_floats 1.5 4.0))

(print (* 2.5 4.0))
(print (+ 1.5 2.5))

(print "string with (parens) inside")
(print "(more")
(print "data)")

(defun get_msg () "hello")
(print (get_msg))
